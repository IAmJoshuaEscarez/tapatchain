
# Backend API URL (deployed .NET backend)
VITE_API_URL=your backend url here


# ── Smart Contract Addresses (Sepolia Testnet) ──
# STEP 1: Deploy TapatChain.sol (Core) in Remix → paste address below
VITE_CONTRACT_ADDRESS=your contract address deployed here

# STEP 2: Deploy TapatChainGate.sol in Remix (pass Core address as constructor arg) → paste address below
VITE_GATE_CONTRACT_ADDRESS=your second gate contract address deployed here


# Cloudflare Turnstile site key (frontend)
VITE_TURNSTILE_SITE_KEY=your turnstile site key here